# EarthPorn Wallpaper (monorepo root)

Приложение находится в каталоге [`earthporn_wallpaper/`](earthporn_wallpaper/README.md).

Локальный клон Flutter SDK (если используется) можно игнорировать в git.
